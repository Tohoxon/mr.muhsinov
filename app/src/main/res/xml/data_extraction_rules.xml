<?xml version="1.0" encoding="utf-8"?>
<!--
   Sample data extraction rules file; see: https://developer.android.com/about/versions/12/backup-restore#xml-changes
   Applicable use cases: A user decides to restore the app to a new device. During the restoration,
   the app prompts the user to review their data and decide which data they want to keep. For example,
   the user wants to restore their progress in a game but not their financial records.
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> and <exclude> tags to control what is backed up.
        <include .../>
        <exclude .../>
        -->
    </cloud-backup>
    <!--
    <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer>
    -->
</data-extraction-rules>
